# 任务3提交完整性检查表

检查日期：2026-08-26

| 验收要求 | 状态 | 证据 |
| --- | --- | --- |
| 固定配时、经典基线、主算法和消融版本 | 已具备 | `最终主线原始指标与轨迹/outputs/ablation_transyt/` 的六种方法、18 份 `metrics.csv` |
| 早高峰、平峰、晚高峰与20路口场景 | 已具备 | 18 份 `cityflow_*.json` 与 `TRANSYT_FRAP消融实验配置.txt` |
| 批量实验、指标计算、绘图、整理和复现命令 | 已补齐 | `scripts/run_batch_experiments.ps1`、`scripts/run_pressure_transyt.ps1`、`scripts/summarize_and_plot.py`、`scripts/summarize_pressure_transyt.py`、`scripts/run_repeated_trials.ps1`、`README.md` |
| 原始 CSV/JSON、时空轨迹 | 已具备 | 每个方法/时段目录的 `metrics.csv`、`trace_*.json`、`live_*.jsonl` |
| 指标曲线、多指标柱状图、结论表、完整报告 | 已具备 | `最终主线_UGAT_TRANSYT_FRAP/` |
| 基线对比和组件消融 | 已具备 | 六组消融与 `TRANSYT_FRAP消融实验汇总.csv` |
| 平峰压力扰动 | 已补齐 | `平峰压力扰动试验/`，保留固定配时和完整 UGAT+FRAP+TRANSYT 的 +10%、+20%、+30% 六组结果 |
| 绝对值、提升率和波动 | 部分具备 | 绝对值/提升率见复算 CSV；当前主线只有单次正式运行，尚无三次重复后的标准差/变异系数 |
| 报告数字可由原始结果复算 | 已具备 | 执行 `python .\\scripts\\summarize_and_plot.py`，重建最终主线目录中的 18 条记录和三张图 |

## 待完成的严格验收项

若验收方明确要求“重复实验波动”，须运行 `scripts/run_repeated_trials.ps1` 并将每组 3 次结果汇总成均值、标准差和变异系数后更新报告。当前 CityFlow 归档结果可复算，但不足以证明跨随机种子的统计波动。
