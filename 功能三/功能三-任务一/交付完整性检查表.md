# 提交材料完整性检查表

| 验收项 | 对应材料 | 状态 |
| --- | --- | --- |
| 固定配时、MaxPressure、TRANSYT 基线实现 | `src/algorithms.py` | 已具备；与任务3最终消融主线版本对齐 |
| 统一基线启动入口 | `scripts/run_task3_aligned_baselines.ps1`、`src/run_cityflow.py` | 已具备 |
| 参数网格、随机种子与依赖 | `configs/xiong_an_20.yaml`、`configs/baseline_parameter_grid.yaml`、`requirements.txt` | 已具备 |
| 路网、流量、原始配时和拓扑 | `data/xiong_an_20/` | 已具备 |
| 结果汇总工具及基线指标表 | `scripts/summarize_baselines.py`、`结果/历史基线指标汇总.csv` | 已具备 |
| 算法、场景适配和公平对比说明 | `docs/公平对比协议与场景适配说明.md`、`docs/复现、动态运行与指标查看指南.md` | 已具备 |
| 场景可独立校验 | `src/validate_scenario.py` | 已通过（20 路口、三时段流量、Excel 对齐） |
| 完整 7500 步 TRANSYT 容器验证 | `结果/最终验证_transyt_morning/metrics.csv` | 已通过；与任务3最终主线早高峰 TRANSYT 指标一致 |
| MaxPressure 三时段运行数值 | `结果/任务3对齐复现/<period>/max_pressure/metrics.csv` | 待按 Docker 批量脚本复算 |

`结果/历史基线指标汇总.csv` 是历史指标的可审阅转录；`结果/最终验证_transyt_morning/` 是本次最终 Docker 验证的完整运行输出，包含指标、轨迹、动态流和本次 CityFlow 配置。
